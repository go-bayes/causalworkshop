% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_policy_learning_workflow.R
\name{fit_policy_learning_workflow}
\alias{fit_policy_learning_workflow}
\title{Fit the PSYC 434 policy-learning workflow locally}
\usage{
fit_policy_learning_workflow(
  n = 5000,
  seed = 2026,
  n_iterations = 100,
  num_trees = 1000,
  parallel = FALSE
)
}
\arguments{
\item{n}{Sample size for \code{\link[=simulate_nzavs_data]{simulate_nzavs_data()}}.}

\item{seed}{Master seed; passed to the simulator and to grf.}

\item{n_iterations}{Bootstrap iterations for
\code{margot::margot_policy_tree_stability()}. Lower for a faster fit;
the canonical cache uses 100.}

\item{num_trees}{Per-outcome tree count for
\code{margot::margot_causal_forest()}.}

\item{parallel}{Forwarded to
\code{margot::margot_policy_tree_stability(parallel = ...)}.}
}
\value{
A named list with \code{models_binary}, \code{policy_tree_stability},
\code{policy_workflow}, and \code{cache_dir = NA_character_}.
}
\description{
Refits the policy-learning multi-outcome causal forest,
policy-tree stability check, and policy workflow on the caller's machine, returning the
same list shape as \code{\link[=load_policy_learning_cache]{load_policy_learning_cache()}}. Use this when you would
rather not deserialise the author's pre-fitted artefacts from Google
Drive — for example, if you would prefer not to run
\code{\link[qs2:qs_deserialize]{qs2::qs_deserialize()}} on a third-party blob, or you want to test a
local change to the simulator.
}
\details{
Expect ~10-25 min on an M-series laptop at defaults.
}
\examples{
\dontrun{
cache <- fit_policy_learning_workflow()
# equivalent to causalworkshop::load_policy_learning_cache(refit = TRUE)
}

}
\seealso{
\code{\link[=load_policy_learning_cache]{load_policy_learning_cache()}} for the Drive-hosted alternative.
}
