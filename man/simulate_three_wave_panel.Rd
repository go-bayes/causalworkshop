% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{simulate_three_wave_panel}
\alias{simulate_three_wave_panel}
\title{Simulate a Three-Wave Panel for Confounding-Control Demonstrations}
\usage{
simulate_three_wave_panel(
  n = 10000,
  beta_A0 = 0.25,
  beta_Y0 = 0.3,
  beta_L0 = 0.2,
  beta_U = 0.1,
  delta_A1 = 0.3,
  delta_Y0 = 0.9,
  delta_A0 = 0.1,
  delta_L0 = 0.3,
  theta_A0Y0L0 = 0.5,
  delta_U = 0.05,
  sigma_Y2 = 0.5,
  seed = 123
)
}
\arguments{
\item{n}{Integer. Sample size (default: 10000).}

\item{beta_A0, beta_Y0, beta_L0, beta_U}{Numeric. Coefficients in the
logistic model for \code{A_1} on \code{A_0}, \code{Y_0}, \code{L_0}, and \code{U} (defaults:
0.25, 0.3, 0.2, 0.1).}

\item{delta_A1, delta_Y0, delta_A0, delta_L0, delta_U}{Numeric. Coefficients
in the linear model for \code{Y_2} on \code{A_1}, \code{Y_0}, \code{A_0}, \code{L_0}, and \code{U}
(defaults: 0.3, 0.9, 0.1, 0.3, 0.05).}

\item{theta_A0Y0L0}{Numeric. Three-way interaction coefficient on
\code{Y_0 * A_0 * L_0} in the outcome model (default: 0.5).}

\item{sigma_Y2}{Numeric. Standard deviation of noise on \code{Y_2} (default:
0.5).}

\item{seed}{Integer. Random seed for reproducibility (default: 123).
Pass \code{NULL} to leave the seed untouched.}
}
\value{
A data frame with columns \code{Y_2} (outcome), \code{A_0} (baseline
treatment), \code{A_1} (focal treatment), \code{L_0} (baseline covariate), \code{Y_0}
(baseline outcome), and \code{U} (unmeasured confounder).
}
\description{
Generates a synthetic three-wave panel (baseline, exposure wave, outcome
wave) used to demonstrate confounding-control strategies and causal-forest
estimation in the simulation guide. Treatment assignment depends on the
baseline covariate \code{L_0}, the prior exposure \code{A_0}, the prior outcome
\code{Y_0}, and an unmeasured confounder \code{U}. The continuous outcome \code{Y_2}
depends on \code{A_1} (the focal treatment), all three baseline measures, an
interaction among \code{Y_0}, \code{A_0}, and \code{L_0}, and \code{U}. The true average
treatment effect of \code{A_1} on \code{Y_2} is \code{delta_A1}.
}
\details{
The data-generating process is:
\itemize{
\item \code{U ~ N(0, 1)}
\item \code{A_0 ~ Bernoulli(plogis(U))}
\item \code{Y_0 ~ N(U, 1)} and \code{L_0 ~ N(U, 1)}
\item \code{A_1 ~ Bernoulli(plogis(-0.5 + beta_A0*A_0 + beta_Y0*Y_0 + beta_L0*L_0 + beta_U*U))}
\item \code{Y_2 ~ N(delta_A1*A_1 + delta_Y0*Y_0 + delta_A0*A_0 + delta_L0*L_0 + theta_A0Y0L0*Y_0*A_0*L_0 + delta_U*U, sigma_Y2)}
}

The true ATE of \code{A_1} on \code{Y_2} is \code{delta_A1} (default 0.3). \code{U} is
included so analysts can compare the bias from omitting it with the bias
from omitting other components of the adjustment set.
}
\examples{
data <- simulate_three_wave_panel()
fit_no_control <- lm(Y_2 ~ A_1, data = data)
fit_interaction <- lm(Y_2 ~ A_1 * (L_0 + A_0 + Y_0), data = data)
coef(fit_no_control)["A_1"]
coef(fit_interaction)["A_1"]

}
