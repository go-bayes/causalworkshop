% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_policy_learning_cache.R
\name{load_policy_learning_cache}
\alias{load_policy_learning_cache}
\title{Load the PSYC 434 policy-learning cache}
\usage{
load_policy_learning_cache(
  url = .policy_learning_cache_url(),
  cache_dir = tools::R_user_dir("psyc434", which = "cache"),
  refresh = FALSE,
  refit = FALSE,
  ...
)
}
\arguments{
\item{url}{Google Drive share URL of \code{lab-09-cache.zip}. Defaults to
the package's canonical URL; can be overridden via the
\code{PSYC434_POLICY_LEARNING_CACHE_URL} environment variable.}

\item{cache_dir}{Directory where the cache is stored on disk.
Defaults to a per-user location returned by \code{\link[tools:R_user_dir]{tools::R_user_dir()}}.}

\item{refresh}{If \code{TRUE}, re-download the cache even if it is already
on disk.}

\item{refit}{If \code{TRUE}, skip the Drive download and refit the cache
locally via \code{\link[=fit_policy_learning_workflow]{fit_policy_learning_workflow()}}. Use this when you would prefer not to
deserialise a third-party blob, or to test a change to the
simulator. Default settings take ~10-25 min on an M-series laptop.}

\item{...}{Forwarded to \code{\link[=fit_policy_learning_workflow]{fit_policy_learning_workflow()}} when \code{refit = TRUE}.}
}
\value{
A named list with elements \code{models_binary},
\code{policy_tree_stability}, \code{policy_workflow}, and \code{cache_dir}.
}
\description{
Downloads (once) and reads the pre-fitted causal-forest artefacts that
the PSYC 434 policy-learning materials build on. The cache lives as a
public Google Drive zip and contains three parquet files written by
\code{\link[margot:here_save_arrow]{margot::here_save_arrow()}}: \code{models_binary.parquet},
\code{policy_tree_stability.parquet}, \code{policy_workflow.parquet}. On first
call the zip is fetched into a per-user cache directory, unpacked,
and the three artefacts are read with \code{\link[margot:here_read_arrow]{margot::here_read_arrow()}}; on
subsequent calls the local cache is read directly.
}
\details{
The instructor shortcut: set \code{PSYC434_POLICY_LEARNING_CACHE} in your
\verb{~/.Renviron} to a local Drive directory holding the three parquet
files, and the function will read them directly without touching
the network.
}
\examples{
\dontrun{
cache <- load_policy_learning_cache()
cache$models_binary
}

}
