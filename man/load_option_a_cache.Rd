% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_option_a_cache.R
\name{load_option_a_cache}
\alias{load_option_a_cache}
\title{Load a PSYC 434 Option A validation cache}
\usage{
load_option_a_cache(
  exposure = c("religious_service", "volunteer_work"),
  cache_root = tools::R_user_dir("psyc434", which = "cache"),
  refresh = FALSE,
  refit = FALSE,
  ...
)
}
\arguments{
\item{exposure}{One of \code{"religious_service"} or \code{"volunteer_work"}.}

\item{cache_root}{Root directory where per-exposure caches are stored
on disk. Defaults to a per-user location under
\code{\link[tools:R_user_dir]{tools::R_user_dir()}}.}

\item{refresh}{If \code{TRUE}, re-download the selected cache.}

\item{refit}{If \code{TRUE}, skip the Drive download and refit locally via
\code{\link[=fit_option_a]{fit_option_a()}}. Use this when you would prefer not to deserialise
a third-party blob, or to test a change to the simulator. Default
settings take ~10-25 min on an M-series laptop.}

\item{...}{Forwarded to \code{\link[=fit_option_a]{fit_option_a()}} when \code{refit = TRUE}.}
}
\value{
A named list with elements \code{models_binary},
\code{policy_tree_stability}, \code{policy_workflow}, \code{cache_dir}, and
\code{exposure}.
}
\description{
Parameterised analogue of \code{\link[=load_policy_learning_cache]{load_policy_learning_cache()}} for the Option A
assessment, where students choose one of two exposures
(\code{"religious_service"} or \code{"volunteer_work"}). Each exposure has its
own zip on Google Drive containing three parquet files written by
\code{\link[margot:here_save_arrow]{margot::here_save_arrow()}}: \code{models_binary.parquet},
\code{policy_tree_stability.parquet}, \code{policy_workflow.parquet}.
}
\details{
The instructor's local Drive mirror is checked first
(under \verb{~/Library/CloudStorage/.../My Drive/courses/26-434/2026/option-a/<exposure>/})
so re-uploads do not stall the workflow while Drive syncs.
}
\examples{
\dontrun{
cache_rel <- load_option_a_cache("religious_service")
cache_vol <- load_option_a_cache("volunteer_work")
}

}
