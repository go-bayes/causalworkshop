% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{simulate_mediation_example}
\alias{simulate_mediation_example}
\title{Simulate Cross-Sectional Mediation Example}
\usage{
simulate_mediation_example(
  n = 1000,
  p = 0.5,
  alpha = 0,
  beta = 2,
  gamma = 1,
  delta = 1.5,
  sigma_L = 1,
  sigma_Y = 1.5,
  seed = 123
)
}
\arguments{
\item{n}{Integer. Sample size (default: 1000).}

\item{p}{Numeric. Probability that \code{A = 1} (default: 0.5).}

\item{alpha}{Numeric. Intercept for \code{L} (default: 0).}

\item{beta}{Numeric. Effect of \code{A} on \code{L} (default: 2).}

\item{gamma}{Numeric. Intercept for \code{Y} (default: 1).}

\item{delta}{Numeric. Effect of \code{L} on \code{Y} (default: 1.5).}

\item{sigma_L}{Numeric. Standard deviation of noise on \code{L} (default: 1).}

\item{sigma_Y}{Numeric. Standard deviation of noise on \code{Y} (default: 1.5).}

\item{seed}{Integer. Random seed for reproducibility (default: 123).
Pass \code{NULL} to leave the seed untouched.}
}
\value{
A data frame with columns \code{A} (binary treatment), \code{L} (mediator),
and \code{Y} (outcome).
}
\description{
Generates cross-sectional data in which \code{L} is a mediator on the path
\code{A -> L -> Y}. With cross-sectional data alone, the analyst cannot
distinguish a fork (\code{A <- L -> Y}) from a chain (\code{A -> L -> Y}); the two
produce the same observed associations. This simulation makes the
ambiguity concrete by generating data from the chain and inviting analyses
that condition on \code{L} (incorrectly treating it as a confounder) versus
omitting \code{L} (correctly treating it as a mediator and recovering the total
effect of \code{A} on \code{Y}).
}
\details{
The data-generating process is:
\itemize{
\item \code{A ~ Bernoulli(p)}
\item \code{L = alpha + beta * A + N(0, sigma_L)}
\item \code{Y = gamma + delta * L + N(0, sigma_Y)}
}

so the total effect of \code{A} on \code{Y} is \code{beta * delta}.
}
\examples{
data <- simulate_mediation_example()
fit_total <- lm(Y ~ A, data = data)             # correct: total effect
fit_blocked <- lm(Y ~ A + L, data = data)        # blocks the mediator
coef(fit_total)["A"]
coef(fit_blocked)["A"]

}
